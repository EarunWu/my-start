{
  "name": "my-start",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "deploy": "wrangler deploy",
    "deploy:check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
